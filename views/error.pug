//- extends layout

//- block content
//-   h1= message
//-   h2= err.status
//-     pre #{err.stack}
extends layout

block content
  h1= `Server Error`
  h2= `Error status code: ${err.status} - Server Error`
  p.error-p= `${err.message}`

  a.home(href=`/`) Home

  div.not-found-stack
    pre #{err.stack}